
\begin{tikzpicture}
\begin{axis}[
    ybar=8pt,              % écarte les bars
    enlarge x limits=0.4,   % top ça ! écarte les côtés
    legend columns=-1,
    legend entries={No coding, Mojette, NS Mojette, ISA-L},
    legend to name=named,
    legend style={font=\footnotesize},
    ylabel={Cycles CPU},
    symbolic x coords={1,2},
    bar width=10pt,
    ymin=0,
    ymax=3500,
    %title={$\mathcal{M}=4096$},
    %x tick label style={ rotate=45 },
    %y tick label style={ rotate=90 },
    xtick=data,%
    xticklabels={encodage $(6{,}4)$, encodage $(12{,}8)$},
    error bars/.cd,
    yminorgrids=true,
    ymajorgrids=true,
    nodes near coords,
    every node near coord/.append style={font=\footnotesize},
    scaled y ticks=base 10:-3,   % insane !
    cycle list = {pattern=north east lines,black!80,black!50,black!30},
    %y filter/.code={\pgfmathparse{(1.8 / #1) * 4096}\pgfmathresult}
]
\addplot+[pattern=north east lines, text=black, error bars/.cd,
y dir=both, y explicit]
coordinates {
    (1, 449)
    (2, 449)
};

\addplot+[fill, text=black, error bars/.cd, y dir=both, y explicit]
table [x={x}, y={mojetteDim}] {expe_data/enc12.dat};

\addplot+[fill, text=black, error bars/.cd, y dir=both, y explicit]
table [x={x}, y={mojetteDidier}] {expe_data/enc12.dat};

\addplot+[fill, text=black, error bars/.cd, y dir=both, y explicit]
table [x={x}, y={isal}] {expe_data/enc12.dat};


\end{axis}
\end{tikzpicture}
